<h1>Группы</h1>

<table>
  <tr>
    <th>Номер</th>
    <th>Пароль</th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.name %></td>
    <td><%= group.psd %></td>
    <td><%= link_to 'Изменить', edit_group_path(group) %></td>
    <td><%= link_to 'Удалить', group, :confirm => 'Вы уверенны?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить новую группу', new_group_path %>
