<%= form_for(@discipline_dictionary) do |f| %>
  <% if @discipline_dictionary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discipline_dictionary.errors.count, "error") %> prohibited this discipline_dictionary from being saved:</h2>

      <ul>
      <% @discipline_dictionary.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
